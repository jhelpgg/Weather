<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="fr.jhelp.weather">

    <application
        android:allowBackup="true"
        android:name=".WeatherApplication"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Weather"
        tools:targetApi="31">
        <activity
            android:name=".ui.activities.locationlist.LocationListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.current.CurrentWeatherActivity" />
        <activity android:name=".ui.activities.addlocation.AddLocationActivity" />
        <activity android:name=".ui.activities.chooseday.ChooseDayActivity" />
        <activity android:name=".ui.activities.daily.DailyWeatherActivity" />
    </application>

</manifest>